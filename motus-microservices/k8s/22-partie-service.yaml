apiVersion: apps/v1
kind: Deployment
metadata:
  name: partie-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: partie-service
  template:
    metadata:
      labels:
        app: partie-service
    spec:
      initContainers:
        - name: wait-postgres
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z postgres 5432; do echo "attente postgres"; sleep 2; done']
      containers:
        - name: partie-service
          image: motus/partie-service:1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8082
          env:
            - name: SPRING_DATASOURCE_URL
              value: jdbc:postgresql://postgres:5432/motus_partie
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: motus-db
                  key: POSTGRES_USER
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: motus-db
                  key: POSTGRES_PASSWORD
            - name: SERVICES_JOUEUR_URL
              value: http://joueur-service:8081
            - name: SERVICES_SCORE_URL
              value: http://score-service:8083
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 60
            periodSeconds: 15
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: partie-service
spec:
  selector:
    app: partie-service
  ports:
    - port: 8082
      targetPort: 8082
